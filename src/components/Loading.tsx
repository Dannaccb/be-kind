const Loading = () => {
  return (
    <div className="flex items-center justify-center" style={{ width: '100%', height: '100%', minHeight: '50vh' }}>
      <div className="spinner"></div>
    </div>
  );
};

export default Loading;

